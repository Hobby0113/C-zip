#include"Main.h"

int main(void)
{
	TILE T[SIZE][SIZE];
	int mine_num;
	TILE *TP = NULL;

	srand((unsigned)time(NULL));
	printf("\n");
	box();
	/*
	mine_num = set_tile(T);
	print_tile(T);
	print_cover();

	play(T,mine_num);
	*/

	return 0;
}
